# Toolkit Dev Instructions

## Syncing

After making changes, sync to all active config directories (BOTH SCRIPTS MUST BE EXECUTED):

```bash
./scripts/sync.sh          # syncs to ~/.claude
./scripts/sync.sh .plaude  # syncs to ~/.plaude
```
